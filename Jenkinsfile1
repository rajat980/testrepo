pipeline {
    agent any
    stages {
        stage('Use AWS') {
            steps {
                
                    sh 'aws cloudformation create-stack --stack-name ec2test --template-body file://cft/ec2.yml --region 'us-east-1'
'
                
            }
        }
    }
}
