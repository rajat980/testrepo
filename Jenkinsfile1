pipeline {
    agent any
    stages {
        stage('Use AWS') {
            steps {
                
                    sh 'aws cloudformation create-stack --stack-name ec2test --template-body cft/ec2.yaml'
'
                
            }
        }
    }
}
