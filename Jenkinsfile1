pipeline {
    agent any
    stages {
        stage('Use AWS') {
            steps {
                withAWS(credentials: 'aws-credentials', region: 'us-west-2') {
                    sh 'aws s3 ls'
                }
            }
        }
    }
}

