pipeline {
    agent any
    environment {
        AWS_CREDS = credentials('my-aws-credentials-id')  // ID of the AWS credentials you created
    }
    stages {
        stage('Deploy') {
            steps {
                withCredentials([aws(credentialsId: 'awsmajor', region: 'us-east-1')]) {
                    script {
                        sh 'aws s3 ls'
                    }
                }
            }
        }
    }
}
